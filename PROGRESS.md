# 実装進捗

> 最終更新: 2026-08-11
>
> **次にやること**: 残りの実機検証（下の「実機検証の結果」で未確認と書いた項目）。
> **検証には必ず `build/comet.app` を使う**（下記）。実装は Phase 6 まで揃っている。
> 状態の確認は `ctrl-alt-shift-r`（全ワークスペースの状態をログに出す）。
>
> ```bash
> ./scripts/build-app.sh debug
> osascript -e 'quit app "AeroSpace"'
> build/comet.app/Contents/MacOS/comet --log-level debug
> ```
> 設計の全体像は [DESIGN.md](DESIGN.md)、使い方は [README.md](README.md)

## 進め方の規約

各 Phase は以下のループが**一巡してはじめて完了**とする。

```
テスト作成 → 実装 → 敵対レビュー → 修正 → 敵対レビュー → 修正
```

- テストは実装より先に書く。コンパイルが通らない red 状態を必ず経由する。
- 敵対レビューは「動くこと」ではなく「壊れ方」を探す。見つけた欠陥は
  深刻度とともに本ファイルに記録し、再発したときに気づけるようにする。
- Phase の完了条件は DESIGN.md §13 に定義済み。**症状 A〜D の解消が最終的な判定基準**であり、
  機能が揃ったことは完了条件ではない。

## 全体

| Phase | 内容 | 状態 | 完了条件 |
|---|---|---|---|
| 0 | 基盤 | **完了** | ホットキーを押すとログが出る / 権限が取れている |
| 1 | ウィンドウ検出と spiral 配置 | **完了** | ウィンドウを開くと自動でタイルされ、閉じると再配置される |
| 2 | BSPツリーとコマンド | **実装完了・実機未検証** | 現行 AeroSpace と同等に動く / **症状B解消** |
| 3 | ワークスペース | **実装完了・実機未検証** | alt-1..0 が動く / **症状C解消** |
| 4 | 高速化の詰めと堅牢性 | **実装完了・実機未検証** | **症状A解消** / ハングアプリ耐性 |
| 5 | 内蔵UI | **実装完了・実機未検証** | **症状D解消** / フォーカス位置が一目で分かる |
| 6 | 常用化 | **実装完了** | 再起動後そのまま常用できる |
| 7+ | 拡張 | 未着手 | 症状が解消してから着手する |

### 潰すべき4症状（プロジェクトの成否判定）

| | 症状 | 解消予定 |
|---|---|---|
| A | 新規ウィンドウがデフォルト位置に一瞬出てから飛ぶ | Phase 4 |
| B | リサイズ連打で追従しない・飛ぶ・戻る | Phase 2 |
| C | ワークスペース切替が遅い・ちらつく | Phase 3 |
| D | 壁紙変更がワンテンポ遅れる | Phase 5 |

---

## Phase 0 — 基盤 【完了】

### 成果物

| モジュール | 内容 |
|---|---|
| `CometSupport` | `LogLevel` / `Log`（stderr + 統一ログ、`@autoclosure` で無効時ゼロコスト）/ `LaunchOptions` / `SingleInstanceLock` |
| `CometInput` | `Hotkey` / `KeySpec`（`"alt-shift-h"` のパーサ）/ `HotkeyManager`（Carbon `RegisterEventHotKey`） |
| `CometAccessibility` | `AXPrivate`（`dlsym` 版 `_AXUIElementGetWindow`）/ `ApplierPool`（PIDごとの直列キュー）/ `AXPermission` |
| `comet` | エントリポイント |
| `scripts/` | `env.sh` / `test.sh` / `build-app.sh` / `make-signing-cert.sh` |

テスト **81件 / 7スイート**。

### 完了条件の検証結果

合成 `CGEvent` でキーを送出して自動検証した（手押しに依存しない）。

| 検証 | 結果 |
|---|---|
| ホットキー押下 → ログ | `ホットキー発火: alt-ctrl-shift-h` → `押下: alt-ctrl-shift-h` |
| アクセシビリティ権限 | 起動時に正しく検出 |
| `_AXUIElementGetWindow` 解決 | 成功（macOS 26.5.2 でシンボル健在） |
| 二重起動の拒否 | 2つ目が `exit=1` でロックエラー |
| 終了ホットキー | `ctrl-alt-shift-q` で正常終了、ロック解放 |
| ロック解放後の再起動 | 成功 |

### 敵対レビューで修正した欠陥

**1回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 高 | `HotkeyManager.start()` が Carbon に `passUnretained(self)` を渡していた。マネージャ解放後にキーが押されると**解放済みメモリを参照**する | `passRetained` + 明示的な `stop()` |
| 高 | `start()` を呼ばずに `register()` すると `RegisterEventHotKey` は成功してキーをシステム全体から奪う一方、配送先が無い。**そのキーだけ無反応**になる | `notStarted` で拒否 |
| 高 | bash 3.2 + `set -u` で**空配列展開が unbound variable エラー**（実測確認） | 要素数で分岐 |
| 中 | `fail()` が stderr のみ。`open` 起動時は stderr が消えるため**致命エラーが不可視** | 統一ログにも出し、`--log-level off` でも致命エラーは抑制しない |
| 低 | `--log-level=` が「不正なログレベル: 」という中身のない診断になる | 欠落として扱う |
| 低 | `HotkeyManager` にテストが1件も無い | Carbon に触れない経路のテストを追加 |

**2回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 中 | **二重起動を検知できない**。残留インスタンスがホットキーを掴んでいると「AeroSpace が原因」と誤診断する | `flock(2)` ベースの `SingleInstanceLock` を追加 |
| 中 | キーコード表に重複があっても**無言で上書き**され、`description` が別のキー名を返すようになる | 全キーの `description` 往復テストを追加 |
| 低 | `make-signing-cert.sh` が partition list を設定せず、署名のたびにキーチェーンのダイアログが出る | 「常に許可」を選ぶ案内を追加 |

---

## Phase 1 — ウィンドウ検出と spiral 配置 【完了】

### 成果物

| モジュール | 内容 |
|---|---|
| `CometCore`（新設） | `Geometry`（座標変換・丸め）/ `Gaps` / `SimpleLayout`（spiral）/ **`FrameCoalescer`** / `WindowClassifier` / `WindowRegistry` / `MonitorManager` / `FrameScheduler` / `Engine` |
| `CometAccessibility` | `AXElement`（Sendable 箱）/ `AXBridge`（バッチ読み・矩形適用）/ `AXObserverHub` |

テスト **172件 / 13スイート**。

### レイアウト

新しいウィンドウが直前に残った領域を半分ずつ取り、分割の向きが縦横交互に切り替わる。
**既定は `dwindle`** — 新しいウィンドウは常に右下に出る。

```
2枚          3枚          4枚            5枚            6枚
┌───┬───┐   ┌───┬───┐   ┌───┬───────┐   ┌───┬───────┐   ┌───┬───────┐
│ A │ B │   │   │ B │   │   │   B   │   │   │   B   │   │   │   B   │
│   │   │   │ A ├───┤   │ A ├───┬───┤   │ A ├───┬───┤   │ A ├───┬───┤
└───┴───┘   │   │ C │   │   │ C │ D │   │   │   │ D │   │   │   │ D │
            └───┴───┘   └───┴───┴───┘   │   │ C ├───┤   │   │ C ├─┬─┤
                                        │   │   │ E │   │   │   │E│F│
                                        └───┴───┴───┘   └───┴───┴─┴─┘
```

「どちら側を取るか」も2回ごとに反転させると、残り領域が 右→下→左→上 と巻き込む
渦（`spiral`）になる。実装は両方あり、`SimpleLayout.Style` で切り替えられる。

最初の分割方向は領域の縦横比で決める（AeroSpace の `default-root-container-orientation = auto` 相当）。
Phase 2 の BSP ツリーはこの形の一般化になるので、丸めの方針ごと引き継げる。

`--preview-layout <n>` でウィンドウに触れずに任意枚数の配置を図示できる。

### 実機での検証結果

AeroSpace を停止して実配置を確認（検証後に復帰済み）。

| 検証 | 結果 |
|---|---|
| 起動時のタイル配置 | 3枚が計算値と**完全一致**（(5,61) 847×1598 / (857,61) 846×1598 / (1708,61) 847×1598） |
| 新規ウィンドウ | Chrome の新規ウィンドウを検出し4分割に再配置 |
| ウィンドウを閉じる | 3分割に復帰 |
| ダイアログの除外 | Chrome の非 `AXWindow` 要素を除外 |
| 起動時カスケード | 配置計算は**1回**のみ、順序はウィンドウ ID 昇順で決定的 |
| 常駐コスト | アイドル時 CPU 0.0% / RSS 33MB |

### 敵対レビューで修正した欠陥

**1回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 中 | **起動時に走査ごとに再配置が走る**。1枚→2枚→3枚と段階的に動いてカスケードが目に見える | 走査が一巡するまで配置を保留し、揃ってから一度だけ行う。応答しないアプリ対策に3秒の打ち切りを併設 |
| 中 | `readWindowAttributes` が要素数 7 をハードコード。属性を足して直し忘れると**全ウィンドウが無言で検出不能**になる | 属性名の配列から要素数を導出 |
| 低 | `spiral` の分割比が未検証で、範囲外の値だと分割の前提が壊れる | `[0, 1]` に丸める |
| 低 | 起動後に生まれたウィンドウの `bundleID` が常に nil | `NSRunningApplication` から補う |
| 低 | ウィンドウの追加/削除にログが無く動作を追えない | debug ログを追加 |

**2回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 中 | **起動時のウィンドウ順序が非決定的**。走査完了順に登録されるため起動のたびに並びが変わりうる | 走査が一巡した時点でウィンドウ ID 昇順に整列 |
| 中 | ウィンドウが領域に対して多すぎると**寸法 0 の目標をアプリに送る**。アプリは最小サイズに戻すだけで結果は重なり | 潰れた矩形は送らず現状維持とし、警告を出す |
| 低 | `removeAll(pid:)` が O(n·m) | `Set` で O(n) に |
| 低 | `FrameScheduler` の「ウィンドウが既に消えていた」経路にテストが無い。ここが詰まると以後そのウィンドウが永久に更新されない | スタブのリゾルバでテストを追加 |

### レイアウト検証で見つかった欠陥と対処

実機で spiral 配置を検証した際に発見したもの。いずれも修正済み。

**1. 適用順序が逆だった（重大）**

`x=1707` にあるウィンドウを `(5,61) 1273×1598` へ動かすと、幅が **853** になっていた。
`853 = 2560 − 1707`、つまり**画面右端までの残り幅**。

当初「拡大時はサイズ→位置」としていたが、これが誤り。

- **AX の位置は画面外へも設定できる**（ワークスペースの画面外退避が成立するのはこのため）
- **AX のサイズは現在位置から画面端までに頭打ちされる**

したがって古い位置のままサイズを設定すると、移動先では収まるはずの幅が切り詰められる。
**順序は常に「位置 → サイズ」**に修正した。これだけで一致するようになり、補正は発生しなくなった。

**2. 目標どおりになったかを確認していなかった**

設定が成功しても実際の矩形は違いうる。適用直後に位置とサイズを**1往復**で読み戻し、
ずれていれば上限つき（既定3回）で投げ直す機構を入れた（設計書 §6.4 の前倒し）。
一致しなければ「アプリ側の制約」と判定して要求値と実測値をログに残す。

**3. アプリの最小寸法を考慮していなかった**

読み戻しで下限に当たったことが分かったら、その寸法を学習してレイアウトに渡すようにした。
分割位置が下限を避けるので、兄弟が譲って間隔が保たれる。
両者の下限を同時に満たせない場合は比例配分で不足を分け合う（片方だけ満たすと、
割を食った側が一方的にはみ出すため）。

実測した下限: **Chrome の最小高さ 469pt**。

### 外部からの変更への反応

レイアウトが唯一の正なので、外部から動かされたら反応する。ただし
**誰が動かしたか・どこが動いたか**で意味が変わるので、順に切り分ける。

```
通知（動いたことしか分からない）
  ↓ 自分の適用中/直後か？          → はい: 無視
  ↓ 前回観測から変わっているか？    → いいえ: 無視
  ↓ マウスの左ボタンが押されているか？
      いいえ（アプリ都合）          → レイアウトへ戻す
      はい（利用者のドラッグ）
        ↓ 動いた辺はすべて分割の境界か？
            いいえ（移動）          → レイアウトへ戻す
            はい（リサイズ）        → 分割の比率を更新し、隣を追従させる
```

動いた辺がどの分割にあたるかは `SimpleLayout.SplitRecord` と照合する。
手前側のウィンドウなら終端が境界そのもの、奥側なら始端が「境界 + 間隔」になる。

#### 追従が暴走した経緯（重要）

最初の実装には**追従が止まらなくなる欠陥**があった。アプリが自分の都合で寸法を
変える（WezTerm は文字セル単位への丸め、Chrome の最小高さ 469、Discord の最小幅 800）
のを「利用者のリサイズ」と誤認し、比率を更新 → 再配置 → またずれる、の往復が
止まらずレイアウトが壊れた。実測でレイアウトの隙間が 5 のはずが 197 になっていた。

歯止めは2つ。

1. **前回観測した矩形から変わっていなければ反応しない。**
   アプリ側の制約でずれたまま安定している状態に反応しないため。
2. **マウスの左ボタンが押されているかで、利用者の操作とアプリ都合の変化を分ける。**
   縁をドラッグするリサイズは必ずボタンを押した状態で起きるが、アプリが自分で
   寸法を変えるときは押されていない。離してから 0.5 秒はドラッグ中として扱う。

#### ドラッグ中の応答性

| 仕組み | 内容 |
|---|---|
| 頻度制限の除外 | ドラッグ中は「2秒で5回」の上限を適用しない。上限に達した時点で追従が止まりカクつくため |
| 当人には触らない | ドラッグ中のウィンドウには適用しない。引っ張り合いになる。離した後に一度だけ全体を整える |
| 読み戻しの省略 | ドラッグ中は適用後の読み戻しをしない。1ウィンドウあたりの往復が 3 → 2 に減る |
| 読み取りの間引き | 読み取りが飛んでいる間は追加で読み取らない |

`TargetFrame.verify` を足し、省略の有無が変わったら別の要求として扱う。
これが無いとドラッグ終了後の仕上げの適用が「同じ矩形」と判定されて発行されない。

#### その他の仕組み

| 仕組み | 内容 |
|---|---|
| 適用中の除外 | 待機中・適用中は自分の作業なので無視（`FrameCoalescer.isActive`） |
| 直後の猶予 | 適用完了から 250ms は無視（`FrameScheduler.isSettling`） |
| 戻し先 | レイアウトが算出した矩形を `desiredFrames` に保持 |
| 分割の比率 | `splitRatios` に保持。手動リサイズで書き換わる |

#### 検証状況

| 項目 | 結果 |
|---|---|
| 移動 → 復元 | Discord を `(400,500) 700x500` へ動かす → `(5,61) 1273x1598` へ復帰 |
| 外周のリサイズ → 復元 | Chrome の下端を引き上げる → 元の高さへ復帰 |
| アプリ都合の変化 → 復元 | スクリプトから幅変更 → 復帰。比率の変動 **0回**で安定 |
| 比率の追従（API経由） | Discord `1273 → 973` → 隣2枚が `x=1283 w=1272` → `x=983 w=1572` へ追従、間隔5を維持 |
| **マウスでの実ドラッグ** | **未検証。** 合成イベントではアプリの UI をクリックしてしまい、端のドラッグにならない。実機で手で試す必要がある |

なお `SimpleLayout` に分割ごとの比率を持たせたことで、Phase 2 の BSP ツリーへ
移す前段は済んでいる。ツリーでは `weights` がこれにあたる。

### 既知の制限

- **下限が幾何的に満たせない配置がある。** 2560×1664 の画面に5枚並べると、
  右下領域（633×796）に Chrome 2枚を入れることになるが、Chrome は
  `469 + 5 + 469 = 943pt` の高さを要求するため物理的に収まらない。
  どの分割方向を選んでも解がなく、現状は比例配分に落ちて重なる。
  **根本的に解くには「最大の実行可能な領域へ置く」配置戦略か、
  スタック/タブ化のどちらかが要る**（未決）。
- **Finder のウィンドウ1件で `_AXUIElementGetWindow` が失敗する。** デスクトップ相当の疑似ウィンドウと思われる。
  スキップされるだけで実害はないが、走査ログに「ID取得失敗 1」として残る。

### 設計書の訂正

DESIGN.md §6.4 に「AX 通知で実結果を観測して**追加の IPC なしで**突き合わせる」と書いたが、**これは誤り**。
`AXWindowMoved` / `AXWindowResized` は「動いた」ことしか伝えず、位置を知るには読み取りが必要になる。
さらに自前の適用でも通知が飛ぶため、無条件に読み戻すと往復が 1.5 倍になる。
Phase 4 では「適用後に1回だけバッチ読み戻して比較する」か「過去に暴れたアプリだけ読み戻す」形にする。

---

## Phase 2 — BSPツリーとコマンド 【実装完了・実機未検証】

### 成果物

| モジュール | 内容 |
|---|---|
| `CometCore/State` | `Tree`（Node / WindowNode / ContainerNode）/ `Normalization` / **`TreeSync`** / `WindowRule` |
| `CometCore/Layout` | `Orientation`（+ `Direction` / `DefaultOrientation`）/ **`LayoutEngine`** / `LayoutPreview` |
| `CometCore/Commands` | `Command`（AeroSpace 互換のパーサ）/ `TreeOperations` |
| `CometConfig`（新設） | `Configuration` / `ConfigLoader`。TOML は [TOMLDecoder](https://github.com/dduan/TOMLDecoder) |
| `CometCore/Engine` | ツリーへの載せ替え、フォーカス追跡、コマンド実行、ウィンドウルールの適用 |

テスト **343件 / 19スイート**。`SimpleLayout` は役目を終えたので削除した（下記）。

ソースを設計書 §10.1 の構成（`State/` / `Layout/` / `Commands/`）に並べ替えた。

### レイアウト

`SimpleLayout` の spiral を `LayoutEngine`（n 分木）に置き換えた。丸めの方針は引き継いでいる。

**分割の境界だけを格子に載せ、最後の子の終端は領域の終端に固定する。**

Phase 1 の dwindle は「新しいウィンドウの入り方」としてツリー上で再現した。

| `[layout] insertion` | 動き |
|---|---|
| `split`（**既定**） | フォーカス中のウィンドウを新しいコンテナに包み、その中を2つで分ける |
| `sibling` | フォーカス中のウィンドウの隣に並べる（AeroSpace と同じ） |

`split` は枚数を増やすと Phase 1 と同じ形になる。`--preview-layout 5` の実測値も一致した。

```
H[1]  →  H[1, 2]  →  H[1, V[2, 3]]  →  H[1, V[2, H[3, 4]]]  →  H[1, V[2, H[3, V[4, 5]]]]
```

**既定を `split` にしたのは判断であり、設計書の完了条件（AeroSpace と同等）とは食い違う。**
Phase 1 で実機検証まで済んでいる形を捨てないほうがよいと判断した。
AeroSpace と揃えたい場合は `insertion = "sibling"` の1行で切り替わる。

### コマンド

| コマンド | 動き |
|---|---|
| `focus <dir>` | 向きが一致する祖先を遡って隣へ。コンテナへ降りるときは**最後にフォーカスした葉**を選ぶ |
| `move <dir>` | 兄弟なら入れ替え（寸法ごと）、隣がコンテナなら中へ、入れ子なら抜けて隣へ |
| `resize <dim> ±N` | **分割の境界を動かす。** 兄弟が同量を譲り、間隔は設定どおり保たれる |
| `join-with <dir>` | 隣と新しいコンテナを作る。取り分は2つの合計を引き継ぐ |
| `layout tiles horizontal vertical` | 親コンテナの向きを巡回 |
| `layout floating tiling` | フローティングとタイルを切り替え |

`workspace` 系は Phase 3。**綴りは知っているが未対応**として区別し、起動時のログに残す。
黙って無視すると、キーが効かない原因が設定側か実装側か分からなくなる。

#### 正規化との衝突を1つ解いた

`layout horizontal` で親コンテナの向きを変えても、正規化の「入れ子は親と逆向き」が
即座に戻してしまい**キーが効かない**。`ContainerNode.isOrientationExplicit` を足し、
**利用者が手で選んだ向きは正規化の対象外**にした。自動で組まれたコンテナは従来どおり整える。

### 設定ファイル

`~/.config/comet/config.toml`。**組み込みの既定を Swift ではなく TOML 文字列で持つ**ので、
既定がそのまま雛形になることが構造的に保証される（`--print-default-config`）。

方針は**「1箇所の誤りで起動を止めない」**。解釈できなかった項目は既定値へ落として
`problems` に理由を積み、起動時のログに出す。TOML 自体が壊れている場合だけ
組み込みの既定に退避する（何を採用すべきか判断できないため）。

### 敵対レビューで修正した欠陥

**1回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 高 | `Normalization` の潰しループが、変更が空振りしたときに**同じ添字を見直し続けて固まる**。木が壊れた瞬間に常駐プロセスがハングする | 同じ添字へ戻る `continue` を「確かにノードが減った」ことに紐づけた。`absorb` も成否を返すようにした |
| 中 | `resize` が `lastLayout` を使うため、直前に `join-with` を打たれていると**新しいコンテナを知らず何も起きない**。再配置は非同期なので必ず起きる | レイアウトは純粋計算なのでコマンド実行時に作り直す |
| 中 | 負のギャップ・`apply-interval-ms = 0` などの設定値が**配置を壊す / CPU を焼く**方向に効く | 範囲で丸め、丸めたことを理由つきで記録 |
| 中 | ルートが `absorb` で子から `isOrientationExplicit` を引き継ぐため、入れ子で選ばれた向きがルートに残り**自動判定が二度と効かなくなる** | 空になった時点で明示指定も捨てる |
| 低 | 挿入の基準が「フォーカス中のウィンドウ」だが、ダイアログにフォーカスがあるとツリーに無く**末尾に付いてしまう** | 最後にフォーカスしたタイル対象へ落とす |
| 低 | 比率の添字参照が不変条件だけを頼りにしていて、崩れたら**添字外参照で落ちる** | 触る前に要素数を確かめ、崩れていれば均等に配り直す |

**2回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 高 | **手動リサイズが二重に適用されうる。** ドラッグ中は同じ辺の通知が何度も届くが、再配置は非同期なので基準の境界座標が古いまま残り、同じ量を繰り返し足す。Phase 1 で踏んだ「追従の暴走」の再来 | どの境界かの**見分けは古いレイアウトで行い、移動量の基準は引き直す**（`Result.boundary(like:)`）。退行検知のため「古い基準なら二重に動く」ことを示すテストも置いた |
| 中 | `move` が入れ子から抜けるとき、隣にコンテナがあっても**中へ入らず新しい列を作る**。浅いところと深いところで同じ操作の意味が変わる | 抜けた先にコンテナがあれば中へ入る（`focus` / 直接の隣の扱いと揃えた） |
| 中 | 走査で ID 取得に失敗しても件数しか残らず、**疑似ウィンドウなのか権限が外れたのか切り分けられない**。実際にこれで詰まった | 失敗の理由を種類ごとに記録し、1枚も認識できなかったときは対処方法まで警告に出す |
| 低 | `layout tiles horizontal vertical` がフローティング中のウィンドウに効かない | `tiles` を含む指定はまずタイルへ戻す |
| 低 | `ctrl-alt-shift-r`（再配置）がツリーの形を出さないため、配置の異常がレイアウト計算かツリー構築かを切り分けられない | ツリーの形をログに出す |

### 見つけた落とし穴

**`#expect` の中で整数式と `CGFloat` を比べると常に不一致になる。**

```swift
#expect(rect.width == 1000 - 5 - 300)   // 695.0 == 695 で失敗する
#expect(rect.width == CGFloat(695))     // 正しく比較される
```

`#expect` の外では正しく `true` になるので、マクロ展開が両辺を独立に型検査することによる。
危険なのは逆向きで、`#expect(x != 1000 - 5)` は常に成立してしまう。
既存のテストを走査して同じ形が他に無いことを確認した。

### `SimpleLayout` を削除した

`LayoutEngine` + `insertion = "split"` が dwindle を完全に再現するため、
役目を終えた。テスト40件も一緒に落としたので総数は一時的に減っている。
`Gaps` のテストだけ `GapsTests` へ移した。

### Phase 2 の未検証項目

**アクセシビリティ権限が外れているため、以下は一切検証できていない。**
ad-hoc 署名の identifier に実行ファイルの内容ハッシュが入るので、
再ビルドで別アプリとして扱われる。`./scripts/make-signing-cert.sh` を実行すれば解消する。

| # | 検証項目 | 手順 |
|---|---|---|
| 1 | ホットキー → コマンドの経路 | `alt-l` でフォーカスが右へ動くか。合成キーは System Events 側の権限が要るため手で押す |
| 2 | **症状B（リサイズ連打）** | `alt-ctrl-l` を2秒押しっぱなしにする。**Carbon の `kEventHotKeyPressed` が OS のキー連射で繰り返し発火するかが未確認**。しなければ設計書 §9.4 のリサイズモードが必要になる |
| 3 | **マウスでの実ドラッグ** | Phase 1 から持ち越し。縁を引いて隣が滑らかに追従するか |
| 4 | `move` / `join-with` / `layout` | ツリーが期待どおり変わるか（`ctrl-alt-shift-r` で形が出る） |
| 5 | ウィンドウルール | システム設定がフローティングになるか |
| 6 | ピクチャ・イン・ピクチャ | dry-run で**タイル対象になっていた**。ルールで除外する必要があるか判断する |

検証で計算そのものは確認できている。dry-run で実ウィンドウ4枚に対し
`H[32, V[2780, H[3544, 3838]]]` を組み、矩形も算出できた。

### 設計書の訂正

**0. §4.3 の「画面外退避」は文字どおりには成立しない。**

「非表示ワークスペースに属するウィンドウを、全モニタの矩形の外側に移動する」と
書いてあるが、macOS はウィンドウを画面外へ出させない（上の「実機検証の結果」を参照）。
できるのは**右下隅に 1pt だけ残して追い込む**ことまで。方式そのものは成立するが、
「完全に見えなくなる」という前提は誤り。

**1. §7.2 の `move` の擬似コードは、ウィンドウではなく祖先コンテナを動かしてしまう。**

擬似コードは `node = parent` と遡ったあとで `parent.children.swapAt(idx, nextIdx)` を
実行するので、**入れ子の中のウィンドウで `move right` を押すと、そのウィンドウを含む
コンテナ全体が動く**。`H[V[1,2], 3]` の 1 で右を押すと `H[3, V[1,2]]` になり、
関係のない 2 まで一緒に移動する。

実装では常に**フォーカス中のウィンドウ自身**を動かす（i3 と同じ）。
遡った先のコンテナで見つけた隣は「そのウィンドウの行き先」として使う。

**2. §5.1 の正規化ルール6（入れ子は親と逆向き）には例外が要る。**

例外なしだと `layout horizontal` が正規化に即座に打ち消され、キーが効かない。
**利用者が明示的に選んだ向きは対象外**にする必要がある。

### 権威モデル（Phase 1 の検証を経て確定）

**レイアウト（分割構造と各分割の比率）が唯一の正であり、ウィンドウの矩形はそこから導出される。**
ウィンドウ側の状態は常に上書き対象で、追従しない場合は補正し、それでも駄目なら
制約として学習してレイアウト側が譲る。Phase 1 で読み戻し・補正・最小寸法学習まで入れたので、
この方向は実装済み。

### 手動リサイズの扱い

**利用者がウィンドウの縁をドラッグしてリサイズしたら、それは「分割境界を動かした」と解釈する。**

- リサイズされたウィンドウの新しい寸法が、その分割の新しい比率になる
- 兄弟はその比率に合わせて再計算され、**間隔は設定値どおりに保たれる**
- 結果として、ウィンドウ単体のサイズを直接変えることはできず、常に境界の移動になる

これを実装するには**分割ごとに比率を保持**する必要がある。Phase 1 の `SimpleLayout` は
比率が固定 0.5 なので表現できない。BSP ツリーの `weights` がまさにこれにあたる。

実装に要るもの:

| 要素 | 内容 |
|---|---|
| 分割ごとの比率 | `ContainerNode.weights`。手動リサイズと `resize` コマンドの両方がここを書き換える |
| 自前の適用と手動操作の区別 | `AXWindowResized` は自前の適用でも飛ぶ。適用直後の通知を無視する仕組みが要る |
| 比率への逆算 | 観測した矩形から、その分割の比率がいくつになったかを求める |

### やったこと

- [x] `Tree` / `Normalization` / `LayoutEngine` + 単体テスト
- [x] focus / move / resize / join-with / layout コマンド
- [x] 手動リサイズ → 比率更新（二重適用の対処込み）
- [x] TOML 読み込みとホットキーの設定駆動化
- [x] ウィンドウルール（floating 指定）

### 完了条件

現行 AeroSpace と同等に動くこと。加えて **症状B（リサイズ連打）が解消していること**。

**まだ満たしていない。** 実装は揃ったが実機検証が権限の問題で止まっている。
症状Bについては、ツリー操作がメモリ操作だけで終わり適用がコアレスされる構造にはなったが、
「押しっぱなしでコマンドが連続発火するか」という前段が未確認。

## Phase 3 — ワークスペース 【実装完了・実機未検証】

### 成果物

| モジュール | 内容 |
|---|---|
| `CometCore/State` | **`Workspace`**（ツリー / `lastFocused` / `isLayoutDirty`）/ **`WorkspaceManager`**（10個・active・back-and-forth） |
| `CometCore/Layout` | `Geometry.stashOrigin`（退避座標） |
| `CometCore/State/WindowRegistry` | `WindowRecord.workspace` と絞り込み（`tiledIDs(in:)` / `visibleIDs(in:)`） |
| `CometCore/Commands` | `workspace <n\|back-and-forth>` / `move-node-to-workspace <n>` |
| `CometCore/Engine` | 退避と復帰、フォーカスの保存・復元、終了前の復帰 |
| `CometConfig` | `[workspaces] count` |

テスト **371件 / 20スイート**。既定のバインドは **32個**（未対応は `fullscreen` と
`move-node-to-monitor` の3個だけ）。

### 実現方式

**画面外退避**（設計書 §4.3）。非表示ワークスペースのウィンドウを
全モニタ union 矩形の**下方** `maxY + 100000` へ動かす。

- 上方（負の方向）へ逃がすとアプリ側で画面内へ引き戻されることがあるので**必ず正方向**
- 寸法 0 の矩形は union に混ぜない（退避先が無意味に遠ざかる）

### 唯一の正をどこに置いたか

**ウィンドウの所属は台帳（`WindowRecord.workspace`）、並びはワークスペースのツリー。**
ツリーは台帳から `TreeSync` で導出する。両方で所属を持つと必ずずれるので、
ワークスペース側は「どのウィンドウが自分のものか」を持たない。

### 退避を再配置に一本化した

設計書 §7.4 は切替の手順として独立した処理を書いているが、実装では
**再配置が「表示側の配置」と「それ以外の退避」の両方を毎回積む**形にした。

```
performRelayout:
  1. 所属ワークスペースが消えたウィンドウを表示中の側へ引き取る
  2. 表示側: ツリーを台帳に合わせ → レイアウト計算 → 目標を積む（先）
  3. 退避側: 表示中でない全ワークスペースのウィンドウを退避先へ積む（後）
  4. 目標を投入してから フォーカスを戻す
```

こうした理由:

- **モニタ付け替えで退避先が動いても勝手に追従する。** 設計書 §7.6 手順5
  「退避中のウィンドウを新しい退避座標へ移動」を忘れる余地that自体が無くなる
- **複数コマンドが1フレームにまとまる。** `["move-node-to-workspace 3", "workspace 3"]` は
  状態を2回書き換えて再配置は1回。中間状態が画面に出ない（設計書 §9.5）
- 毎回すべて積み直すが、合成器が「変化なし」を落とすので **IPC は増えない**

### サイズ設定の省略（症状C の対策）

復帰時に**位置だけ**を設定すれば1ウィンドウあたりの IPC が2回から1回になる。
ただし省けるのは「復帰直後」かつ「非アクティブ中に何も起きていない」ときだけ。

| 状況 | サイズを設定するか |
|---|---|
| 通常の再配置 | する |
| 復帰直後 + `isLayoutDirty` | する |
| 復帰直後 + レイアウト不変 | **しない**（IPC 半減） |

`isLayoutDirty` は、ツリーが変わった / ウィンドウを移した / モニタ構成が変わったときに立つ。

### 敵対レビューで修正した欠陥

**1回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 高 | **終了すると非表示ワークスペースのウィンドウが画面外に残る。** 利用者からは「ウィンドウが消えた」ようにしか見えず、アプリのウィンドウメニューから呼び戻すしかない | 終了前に退避中のウィンドウを画面へ戻す。適用は非同期なので上限つきで待つ（応答しないアプリで終われなくならないよう 0.4 秒） |
| 高 | **戻す位置が分からないウィンドウを退避すると永久に失う。** 退避すると `observedFrame` が退避先で上書きされるため、次の再配置で「退避先が元の位置」として記憶される | 矩形が分からないウィンドウは退避を見送る |
| 中 | **フォーカスの復元が再配置より先に走る。** まだ退避先にいるウィンドウをアクティブにしてしまい「アプリは前面だが見えない」状態になる | 目標を積んだあとに戻す（`pendingFocusRestore`） |
| 中 | 設定でワークスペースの数を減らすと、行き場を失ったウィンドウが**表示も退避もされないまま画面に残る** | 所属先が無いウィンドウを表示中の側で引き取る |
| 低 | フォーカスの行き先が台帳の登録順で決まり、「左上のウィンドウ」にならない | ツリーの並び順の先頭にする |

**2回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 中 | **Cmd+Tab で非表示ワークスペースのアプリへ切り替えると、コマンドが画面外のウィンドウを操作する。** `alt-shift-f` や `move-node-to-workspace` が見えていないウィンドウに当たり、何が起きたか分からない | 「フォーカス中のウィンドウ」を対象にするコマンドは表示中のワークスペースのものに限る |
| 低 | 状態を追う手段が表示中のツリーしか無く、配置の異常が「ツリーの組み方」「所属」「退避」のどれなのか切り分けられない | `ctrl-alt-shift-r` が全ワークスペースの状態を出す |

なお **Cmd+Tab そのものの体験改善**（非表示ワークスペースのアプリが前面に来たら
そのワークスペースへ自動で切り替える。設計書 §12.6 の `focus_follows_activation`）は
Phase 4 の項目なので入れていない。

### Phase 3 の未検証項目

Phase 2 と同じく**アクセシビリティ権限が外れているため実機検証できていない。**

| # | 検証項目 | 手順 |
|---|---|---|
| 1 | **症状C（切替の遅さ・ちらつき）** | 4枚の WS1 ↔ 3枚の WS2 を往復。1枚ずつ現れないか |
| 2 | 高速切替 | `alt-1`〜`alt-5` を素早く連打。追従するか、フォーカスが失われないか |
| 3 | フォーカス復元 | WS1 でウィンドウB → WS2 → WS1 でBに戻るか |
| 4 | サイズ設定の省略 | 何もせず往復したときに位置だけの適用になっているか（`--log-level trace`） |
| 5 | `move-node-to-workspace` | `alt-shift-3` でウィンドウが移動して追従するか。中間状態が見えないか |
| 6 | back-and-forth | `alt-tab` で直前へ戻るか、往復するか |
| 7 | **終了時の復帰** | 退避したまま `ctrl-alt-shift-q` して、全ウィンドウが画面に戻るか |
| 8 | 退避の押し戻し | 非表示のウィンドウをアプリ側が動かしても画面に現れないか |

### 完了条件

`alt-1..0` / `alt-shift-1..0` が動くこと。加えて **症状C が解消していること。**

**まだ満たしていない。** ホットキーの登録は 32/32 成功しているが、
実際の切替を観測できていない。

---

## Phase 4 — 高速化の詰めと堅牢性 【実装完了・実機未検証】

### 成果物

| モジュール | 内容 |
|---|---|
| `CometSupport` | **`TimingRecorder`**（アプリ別のレイテンシ集計。環状バッファで上限つき） |
| `CometAccessibility` | `AXEnhancedUserInterface` の無効化と復帰 / 適用の計測 |
| `CometCore/FrameScheduler` | **dry-run の関門** / 追従を諦めたときの通知 |
| `CometCore/Engine` | 新規ウィンドウの最優先適用 / フローティング降格 / `focus_follows_activation` / 終了時の後片付け |
| `CometSupport/LaunchOptions` | `--run <command>`（ホットキーを押せない環境での検証口） |
| `CometConfig` | `[performance] disable-enhanced-ui` / `[debug] timing` / `[workspaces] focus-follows-activation` |

テスト **392件 / 21スイート**。

### 症状A（新規ウィンドウのちらつき）への対策

SIP 有効下では他プロセスのウィンドウの初回描画を止められないので、
**「通知を受けてから適用が終わるまで」を短くするしかない**（設計書 §3.2）。

| 対策 | 状態 |
|---|---|
| アプリ起動時に AXObserver を事前登録 | Phase 1 で実装済み |
| 属性を1往復で読む | Phase 1 で実装済み |
| 新規ウィンドウは**次のランループを待たずにその場で配置** | Phase 4 で追加（`relayout(immediately:)`） |
| 新規ウィンドウを**適用順の先頭へ回す** | Phase 4 で追加（`priorityWindows`） |

### `AXEnhancedUserInterface`

`true` のアプリではリサイズに追加処理やアニメーションが挟まって遅くなる。
皮肉なことに **WM 自身が AX 接続した時点で立つ**ことがある。

設計書は「操作の直前に落として直後に戻す」としているが、それだと**1回の適用につき
IPC が2回増える**。**管理している間は落としたままにし、手放すとき（または終了時）に
戻す**方式にした。VoiceOver を併用する場合は `disable-enhanced-ui = false`。

### フローティング降格に閾値を置いた

設計書 §12.4 は「補正3回で一致しなければ floating へ降格」としているが、
**そのままだと WezTerm が即座に浮く。** 文字セル単位への丸めで必ず数 pt ずれるため。

降格は**ずれが 50pt を超えたときだけ**にした。丸めや最小寸法は数十 pt で収まるので、
これで「AX を無視している」ものだけを拾える。降格時のログには
`window-rule` の書き方を添えて、恒久的な対処へ誘導する。

副作用として、**幾何的に最小寸法を満たせない配置**（Phase 1 の既知の制限。
2560×1664 に5枚で Chrome 2枚が入らない件）でも降格が働き、重なりの代わりに
1枚が浮くようになった。

### 敵対レビューで修正した欠陥

**1回目**

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 高 | **`--dry-run` がウィンドウを実際に動かしていた。** 外部変更の戻し経路（`scheduler.reapply`）が dry-run を見ていなかった。他の WM が動く環境での安全な検証という目的そのものが崩れていた | 呼び出し側で止めると経路が増えたときに必ず漏れるので、**関門を `FrameScheduler` 一箇所に置いた** |
| 中 | **`[debug] timing` が `[performance]` を書かないと読まれない。** セクションの有無が別セクションの読み取りに結び付いていた | 独立して読む。セクション間に依存を作らないテストも追加 |
| 中 | 終了時に無効化した `AXEnhancedUserInterface` が戻らない。comet を止めたあとも支援技術向けの挙動が無効なまま残る | `prepareForTermination()` で退避の復帰とまとめて戻す |
| 低 | 設定ファイルを書くと既定のバインドが**全て消える**のに何も言わない。`[gaps]` だけ書いた設定でキーが全部死ぬ | バインドが 0 件なら警告を出し、`--print-default-config` を案内する |

### 未検証項目

**アクセシビリティ権限が不安定**で、実機検証が通し切れていない。
ad-hoc 署名の identifier は `comet-<内容ハッシュ>` なので**再ビルドごとに別アプリ**になる
（実測: `comet-5555494413ac...` → `comet-5555494470ef...`）。検証中に一度だけウィンドウを
3枚認識できたが、再ビルド後は再び認識できなくなった。

| # | 検証項目 | 手順 |
|---|---|---|
| 1 | **症状A** | WezTerm / Chrome の新規ウィンドウ。デフォルト位置が見えないか |
| 2 | 計測 | `[debug] timing = true` にして `ctrl-alt-shift-t`。遅いアプリが見えるか |
| 3 | `AXEnhancedUserInterface` | Discord（Electron）の適用レイテンシが有効/無効で変わるか |
| 4 | フローティング降格 | 寸法を無視するアプリが浮くか。**WezTerm が浮かないこと** |
| 5 | focus_follows_activation | 非表示ワークスペースのアプリを Cmd+Tab で選ぶと切り替わるか |
| 6 | ハングアプリ耐性 | 重い処理でアプリを固まらせ、他のウィンドウが操作できるか |
| 7 | 常駐コスト | 無操作で10分放置。CPU ほぼ 0%、メモリが増え続けないか |

コマンドの経路は `--run` で通した（dry-run、ウィンドウ 0 枚）。
`workspace 1 → 2 → 1 → 3`、`back-and-forth` で `3 → 1` まで想定どおり動いた。

---

## Phase 5 — 内蔵UI 【実装完了・実機未検証】

### 成果物

| モジュール | 内容 |
|---|---|
| `CometCore` | `RGBAColor`（`#RGB` / `#RRGGBB` / `#RRGGBBAA` のパーサ）/ `BorderStyle` / `IndicatorStyle` |
| `CometDecoration`（新設） | **`WallpaperService`** / **`FocusBorder`** / **`WorkspaceIndicator`** / `DecorationController` |
| `CometConfig` | `[border]` / `[indicator]` / `[wallpaper]` |

テスト **423件 / 23スイート**。

### 依存の向き

**`CometCore` は AppKit の表示を知らない。** Engine はクロージャで合図だけを出し、
配線は `main.swift` が行う。

```
Engine.onFocusedFrameChanged ─┐
Engine.onWorkspaceChanged   ──┴→ DecorationController → 枠線 / インジケータ / 壁紙
```

### 症状D（壁紙変更の遅れ）への対策

現行の `osascript` 経由は **0.2〜1.5 秒**かかる（bash 起動 + AppleScript 処理系の初期化 +
System Events への AppleEvent）。`NSWorkspace.setDesktopImageURL` をプロセス内で直接呼ぶ。

**壁紙とインジケータはウィンドウ移動の発行より前**に更新する。どちらも自プロセス側の
処理なので即座に終わり、切替が速く見える。

実在の検証は**起動時に一度だけ**。切替のたびに確かめるのは無駄なので、
存在しないパスは起動時に警告して捨てる。

### 枠線は AX の完了を待たない

**目標矩形が決まった時点で即座に動かす。** 枠線は自プロセスのウィンドウなので即座に動く。
結果として「枠線が先に着地し、ウィンドウがそれに収まる」ように見え、AX の遅延が
視覚的に隠れる（設計書 §8.2）。目標に届かなかった場合は読み戻した実測値へ合わせ直す。

枠線ウィンドウは対象より**線の幅だけ外側**に広げる。同一にすると枠が中身に被る。
gaps 5pt なら width 2pt がギャップの中に収まる。

### 未決事項が1つ解決した

**`~/Pictures/wallpapers/` は実在しない。** 現行の `wallpaper_config.sh` は
`wallpaper1.jpg`〜`wallpaper5.jpg` を参照しているが、`~/Pictures` の中身は
`Photos Library.photoslibrary` だけだった。

つまり**現行環境では壁紙の切替は元々効いていない**（症状Dは「遅い」というより
「何も起きていない」可能性がある）。comet は未設定のワークスペースでは壁紙を変えないので、
画像を置くまでは何もしない。置いたときに初めて効く。

### 敵対レビューで修正した欠陥

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 中 | `[border] color-unfocused`（タイル全部に枠を描く）を**設定から読むのに実装が無い**。書いても効かないことが分からない | 設定モデルから落とし、書かれていたら「未対応」として報告する |
| 低 | 内蔵UI を dry-run で動かしていなかった。他のアプリのウィンドウには一切触らないので、動かしたほうが**枠線が目標位置を指す**ぶん配置計算の目視確認になる | dry-run でも配線する |

### 未検証項目

権限が不安定なため、枠線が実ウィンドウに追従する様子は確認できていない。
**表示の経路そのものは通した**（メニューバー項目・HUD の生成、ワークスペース切替、
正常終了までクラッシュなし）。

| # | 検証項目 | 手順 |
|---|---|---|
| 1 | **症状D** | 壁紙を `~/Pictures/wallpapers/` に置いて設定し、切替と同時に変わるか |
| 2 | 枠線の追従 | フォーカスを移して枠線が付いてくるか。ギャップの中に収まるか |
| 3 | 枠線が先に着地するか | リサイズ時に枠線が先、ウィンドウが後に見えるか |
| 4 | HUD | 切替で中央に番号が出て消えるか。連打で重ならないか |
| 5 | メニューバー | 番号が出るか、切替で変わるか |
| 6 | 視差効果を減らす | 有効時に HUD のフェードが省かれるか |

---

## Phase 6 — 常用化 【実装完了】

### 成果物

| モジュール | 内容 |
|---|---|
| `CometConfig` | **`ConfigWatcher`**（ファイル + 親ディレクトリの監視）/ `start-at-login` |
| `CometCore` | `close-window` / `reload-config` コマンド |
| `CometAccessibility` | `AXBridge.close`（閉じるボタンを押す） |
| `comet` | 設定のホットリロード、`SMAppService` によるログイン起動 |
| `README.md` | セットアップ手順、必須システム設定、移行時に消すもの |

テスト **433件 / 24スイート**。

### 設定のホットリロードは**実機で検証済み**

権限に依存しないので、ここは実際に確かめられた。

| 保存の仕方 | 結果 |
|---|---|
| 上書き保存（内容だけ変わる） | 読み直し成功、ホットキー 1 → 2 個に更新 |
| rename での差し替え（vim / home-manager 方式） | 読み直し成功、ホットキー 2 → 3 個に更新 |

**ファイルと親ディレクトリの両方を見ないと取りこぼす。**
最初はディレクトリだけを見ていて上書き保存を取りこぼした。

| 変更の仕方 | 観測できる場所 |
|---|---|
| エディタが内容だけ書き換える | **ファイル**への書き込み |
| 一時ファイルを作って rename する | **ディレクトリ**への書き込み |
| home-manager がシンボリックリンクを張り替える | **ディレクトリ**への書き込み |

差し替えられると inode が変わるので、ディレクトリの変更を受けたら
**ファイル側の監視も張り直す**。これを忘れると2回目以降を取りこぼす（テストで固定した）。

### 再読込で変えられるもの・変えられないもの

**ツリーの形は保つ。** 設定を触るたびに配置が崩れると常用できない。

| 反映される | 再起動が要る |
|---|---|
| gaps / 正規化 / 挿入方法 / 既定の向き | `[workspaces] count` |
| ウィンドウルール / focus-follows-activation | `[performance]` / `[debug] timing` |
| 枠線 / インジケータ / 壁紙 | `start-at-login` |
| キーバインド（全解除して張り直す） | |
| ログレベル | |

再起動が要るものが変わっていたら**起動時にではなく再読込時に**警告する。
黙って無視すると「設定したのに効かない」で詰まる。

TOML が壊れている間は**現状の設定を使い続ける**。編集途中の構文エラーで
設定が吹き飛ばないようにするため。

### 利用者側の作業として残るもの

コードでは片付かないので README に手順として書いた。

- `./scripts/make-signing-cert.sh`（キーチェーンのパスワード入力が要る）
- 必須のシステム設定4項目（個別の操作スペース / 並べ替え / Stage Manager / 視差効果）
- AeroSpace の停止とアンインストール
- `~/.config/aerospace/wallpaper.sh` の呼び出しの削除
- Hammerspoon の「修飾キー + BS でウィンドウを閉じる」の削除（`close-window` と重複）
- home-manager で `~/.config/comet/config.toml` を管理する

### 敵対レビューで修正した欠陥

| 深刻度 | 欠陥 | 対処 |
|---|---|---|
| 高 | **ディレクトリだけを監視していて上書き保存を取りこぼす。** home-manager のことだけを考えていた。エディタで直接編集する方が普段の使い方 | ファイルも併せて監視し、差し替え時に張り直す |
| 中 | 再読込で `[performance]` などが反映されないのに何も言わない | 再起動が要る項目が変わっていたら警告する |
| 低 | `close-window` / `reload-config` が「未対応」のままだった | 実装してパーサから外した |

---

## 実機検証の結果（Phase 2〜6 まとめ）

AeroSpace を停止し、`build/comet.app` を実ウィンドウ4枚に対して動かした。**検証後に復帰済み。**

### 通ったもの

| 検証 | 結果 |
|---|---|
| 起動時のタイル配置 | 4枚を認識し dwindle で `H[32, V[2780, H[3544, 4123]]]` を構築 |
| 実座標の一致 | `(5,61) 1273x1598` / `(1283,61) 1272x797` / `(1283,863) 1272x796` が**計算値と完全一致** |
| **フローティング降格** | PiP ウィンドウが 633x796 の要求を無視して 1415x796 を返し、補正3回のあと降格。ツリーが `H[32, V[2780, 3544]]` に整った |
| ツリー操作 | `join-with` → 正規化で吸収（ルート直下の全員をまとめると元に戻る）/ `layout tiles horizontal vertical` でルートが V になり、入れ子が H に反転 |
| ワークスペース切替 | 3枚を退避 → 復帰。`move-node-to-workspace 2` → `workspace 2` → `back-and-forth` まで想定どおり |
| 終了時の復帰 | 退避していたウィンドウが画面へ戻った |
| **計測** | Discord (Electron) が `setSize p95=70.9ms` / `setPosition p95=37.9ms` で最も遅い。WezTerm 8.4ms、Chrome 3.1ms |
| 設定のホットリロード | 上書き保存と rename の両方で読み直し（権限に依存しないので単体でも検証済み） |

### 見つけた重大な誤り: **ウィンドウは画面外へ出せない**

設計書 §4.3 と Phase 1 の記録は「AX の位置は画面外へも設定できる」としているが、
**これは誤り。** macOS はどの方向へもウィンドウを引き戻す（アプリ側の
`constrainFrameRect` が働く）。実測値:

| 要求 | 実際（2560×1664 の画面） |
|---|---|
| `(0, 101664)` | `(0, 1618)` — **全幅 46pt の帯が画面下に残る** |
| `(0, -100000)` | `(0, 56)` — メニューバーの下 |
| `(102560, 101664)` | `(2520, 1618)` — 右下に 40pt × 46pt |
| **`(2559, 101664)`** | **`(2559, 1618)`** — 1pt × 46pt |

つまり「画面外へ出す」ことはできず、**残る面積を最小にする**のが限界。

分かれ目は**画面と1pt でも重なる位置を要求するかどうか**。完全に外を要求すると
引き戻しが働いて 40pt 残るが、`maxX - 1` を要求すると**そのまま通って 1pt で済む**。

AeroSpace も同じ位置（実測 `(2559, 1618)`）に落ちるので、これが現実的な上限。
`Geometry.stashOrigin` を「x は 1pt 重ね、y は遠くへ投げる」に直し、実測で一致を確認した。

**この 1pt × 46pt の角は、アプリ単位の非表示に切り替えることで消せた**（次節）。

### アプリ単位の非表示に切り替えた（既定）

隅寄せの限界（1pt × 46pt が残る）を、**アプリごと非表示にする**（`Cmd+H` 相当、
`NSRunningApplication.hide()`）ことで解消した。実機で確認済み。

| 検証 | 結果 |
|---|---|
| 空のワークスペースへ切替 | 3アプリすべて非表示。**隅寄せ 0 枚**（角も残らない） |
| 元のワークスペースへ戻す | 3アプリすべて表示に戻る |
| comet 終了時 | 非表示にしたアプリを全て戻す |
| 検証後のデスクトップ | 非表示のまま残るアプリなし、全ウィンドウ画面内 |

**利点**

- **完全に消える。** 1pt も残らない
- **Mission Control に出ない**（隅寄せでは出ていた）
- **AX の往復が要らない。** `hide()` はローカル処理。隅寄せは N 枚ぶんの位置設定が必要
- 戻すときもレイアウトが変わっていなければ位置設定が 0 回（`isLayoutDirty` が false）

**制約: 粒度がアプリ単位**

そのアプリの管理下ウィンドウが**すべて**非表示ワークスペースにあるときしか使えない。
表示中のワークスペースにもウィンドウを持つアプリ（Chrome を ws1 と ws2 で使うなど）は
**自動的に隅寄せへ落ちる**。判定は `HidePlanner` に純粋関数として切り出し、
「表示中のウィンドウを持つアプリは絶対に隠さない」を含む13件のテストで固めた
（誤ると見ているワークスペースのウィンドウまで消えるため）。

`[workspaces] hidden = "off-screen"` で従来の隅寄せに戻せる。

**残る副作用**

- **Cmd+Tab には非表示アプリも出る。** 選ぶとアプリごと表示に戻り、
  `focus_follows_activation` がそのワークスペースへ移る（自己修復するが一瞬ちらつく）
- Dock には「実行中」の点が付いたままになる（`Cmd+H` と同じ挙動）

### まだ確認できていないこと

| # | 検証項目 | なぜできていないか |
|---|---|---|
| 1 | **症状B（リサイズ連打）** | Carbon の `kEventHotKeyPressed` がキー連射で繰り返し発火するかが未確認。手で押しっぱなしにする必要がある |
| 2 | **マウスでの実ドラッグ** | 合成イベントでは端のドラッグにならない |
| 3 | **症状A（新規ウィンドウのちらつき）** | 目視でしか判定できない |
| 4 | 枠線・HUD・インジケータの見え方 | 目視でしか判定できない |
| 5 | 症状D（壁紙） | `~/Pictures/wallpapers/` に画像が無い |
| 6 | ハングアプリ耐性 / 常駐コスト10分 | 時間のかかる検証 |

---

## 環境で判明した制約

| 事項 | 内容 |
|---|---|
| **Xcode 未インストール** | Command Line Tools のみ。`xcodebuild` は使えないが SPM ビルドと手動バンドル化で足りる |
| **`Testing.framework` の探索** | CLT 内にあるが SPM が自動で見つけない。`scripts/env.sh` が `-F` と rpath 2本を補う。テストは **`./scripts/test.sh`** を使う（`swift test` 直叩きは失敗する） |
| **`XCTest.framework` は無い** | CLT に同梱されていない。テストは Swift Testing 一本 |
| **`/bin/bash` は 3.2** | `set -u` のもとで空配列の `"${arr[@]}"` 展開がエラーになる。`${#arr[@]}` で分岐すること |
| **署名 ID が未作成** | 現在 ad-hoc 署名のため**再ビルドのたびにアクセシビリティ権限が外れる**。`./scripts/make-signing-cert.sh` を一度実行すると解消（キーチェーンのパスワード入力が必要なため手動） |
| **AeroSpace が稼働中** | 開発中は停止しないとホットキーとウィンドウ配置を奪い合う。Phase 0 の既定バインドを `ctrl-alt-shift-*` にしているのはこのため。なお `alt-h` 等は AeroSpace が動いていても**登録に成功する**（16/16 成功を実測）ので、両方が反応してしまう |
| **素の実行ファイルは権限を保てない（重要）** | `.build/debug/comet` の ad-hoc 署名は identifier が `comet-<内容ハッシュ>` になるため、**再ビルドごとに TCC から別アプリとして扱われる**。このとき `AXIsProcessTrusted()` は `true` を返し `AXWindows` も読めるのに、`_AXUIElementGetWindow` だけが `kAXErrorIllegalArgument`(-25201) を返す、という分かりにくい壊れ方をする |
| **アプリバンドルなら権限が続く（実測）** | `build/comet.app` は `codesign --identifier local.comet` で identifier が固定されるため、**ad-hoc 署名でも再ビルドをまたいで権限が維持される**。実測で3枚のウィンドウを認識できた。**実機検証は必ずアプリバンドルで行う。** `make-signing-cert.sh` は「ダイアログを減らす」ための改善で、必須ではない |
| **合成キーの経路** | `osascript` の System Events 経由のキー送出は、送る側に別途アクセシビリティ権限が必要。Phase 0 で使った `CGEvent` 直接送出とは別の権限になる。押せない環境では `--run <command>` でコマンドの経路を通せる |
| **増分ビルドが壊れる** | **既存の struct にフィールドを足すと、テストバンドルが signal 11 で落ちる。** 落ちる場所は毎回変わり、変更したコードとは無関係なテスト名が出る（`Bool` 2つを足した純粋な struct で再現）。`swift package clean` で解消する。原因はレイアウト変更に一部のモジュールが追従しないため |

## 未決事項

| 事項 | 判断が必要な時期 |
|---|---|
| ~~`~/Pictures/wallpapers/` の実在~~ | **解決: 存在しない。** 画像を置くまで壁紙の切替は起きない |
| macOS 26 で `NSWorkspace.setDesktopImageURL` が期待通り動くか。ダメなら最背面に自前の壁紙ウィンドウを置く方式へ（設計書 §12.5 の方式2） | 壁紙を置いてから確認 |
| Hammerspoon の `alt-f` / `alt-d`（アプリ・ウィンドウ巡回）を comet 側へ移すか | Phase 6 |
